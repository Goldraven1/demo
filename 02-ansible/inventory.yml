---
all:
  hosts:
    echo_server:
      ansible_host: 82.202.158.202 
      ansible_user: user1
      ansible_ssh_private_key_file: ./ssh_key  # Путь к приватному ключу в текущей директории
      ansible_become: yes
      ansible_become_method: sudo
  vars:
    docker_hub_password: "Razvilka1+2"
